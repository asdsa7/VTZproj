@model VTZ.Domain.Entities.Task

<div class="well">
    <h3>
        <strong>@Model.DocumentName</strong>
        <span class="pull-right label label-primary">@Model.DocNo.ToString()</span>
    </h3>
    <span class="lead">@Model.DocNo</span>
</div>